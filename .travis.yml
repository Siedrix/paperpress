language: node_js
node_js:
  - "4"
  - "5"
install:
  - npm install
before_script:
  - mocha --version
  - sleep 3 # give mocha some time to execute.
after_script:
  - NODE_ENV=test istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R nyan && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage
